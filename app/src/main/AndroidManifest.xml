<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidfinalgroupproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DictionaryWordDetail"></activity>
        <activity android:name=".DictionarySavedWords" />
        <activity android:name=".DictionaryInfo" />
        <activity android:name=".NewYorktimes_ArticleActivity"
            android:theme="@style/AppTheme.NoActionBar"  />
        <activity android:name=".NewYorkTimes"
            android:theme="@style/AppTheme.NoActionBar"  />

        <activity android:name=".Dictionary" />
        <activity android:name=".PlaneTracker" />
        <activity android:name=".NewsFeed" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>